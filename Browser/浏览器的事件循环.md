宏任务队列和微任务队列只存在于老版浏览器了，浏览器现在没有宏任务的说法了，但是一定有微任务队列，任务没有优先级，但是任务队列有优先级，微任务队列优先级最高，Promise().then() then内的函数一定是微任务队列内的任务，还有计时器队列(优先级中等)，交互队列(点击事件等，高优先级)

主渲染进程，消息队列，主渲染进程执行完毕之后开始从消息队列里拿消息，消息队列内的消息根据微任务队列、交互队列、计时器队列生成的任务进行排序，每一个任务都是一个函数